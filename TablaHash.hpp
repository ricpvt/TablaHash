#ifndef TABLAHASH_HPP
#define TABLAHASH_HPP


class TablaHash
{
    public:
        TablaHash();
        ~TablaHash();
        TablaHash(const TablaHash& other);
        TablaHash& operator=(const TablaHash& other);

    protected:

    private:
};

#include "TablaHash.hpp"

#endif // TABLAHASH_HPP
